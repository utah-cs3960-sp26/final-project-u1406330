Make placed resources correctly size with their size attribute. Right now the mock placement size scales with attribute, but the actual placed resource does not. see rose.png for reference, it shows two resources (one tiny and one huge) but both are the same size.

The transparent mock placement for placing resources is just a circle (much like a geode's) It should show a transparent model of the resource being placed.

Things dont quite scale correct in regards to geode cracking timing, the common ones should take 15 seconds, and the rarer ones should scale off of that.

Resource attributes are very hard to distinguish between right now. A resource with an attribute only has a small sparkle effect added too it, it does not even necessarily match its attribute. Work on this and make it look better.

If it is possible, add audio (think slot machine esc) when geodes are found (rarer = louder/more allaborate) and add audio to resource rolls and attribute rolls(cracked geodes).

Add a menu in the bottom right of the screen called (Resource Guide) in the same bubbly kid friendly style as the vault and coin counter. This should show a chart of rarities, most common at top. most rare at bottom, and show their rarity percentages. Make sure it is color coded and very poppy.

Resource names still do not show up when you mouse is over them. fix this